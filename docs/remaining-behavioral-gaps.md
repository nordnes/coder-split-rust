# Remaining Behavioral Gaps

> **Scope:** Now that every Go route has a Rust counterpart (326/326, 100%),
> this document enumerates the remaining *behavioral* gaps — places where the
> Rust handler is reachable and returns a sane shape, but the underlying
> semantics still differ from the Go reference in `coder/`. It is the
> authoritative gap inventory; the per-route `crates/coder-server/PARITY_MATRIX.md`
> is a generated/older view that has drifted in places and should not be used
> to reason about what still needs work.

Categories are listed from most user-visible to most subsystem-specific, with a
priority matrix at the end.

## 0. Route-Level Parity Status

| Scope | Ported | Total | % |
|-------|-------:|------:|-----:|
| OSS | 229 | 229 | 100% |
| Enterprise | 87 | 87 | 100% |
| **Combined** | **326** | **326** | **100%** |

`docs/parity-matrix.md`, `docs/parity-matrix-enterprise.md`, and
`docs/parity-matrix-all.md` are generated by `apps/coder-parity` and reflect
that 100% route coverage. This document is about the work that *remains*
inside those already-ported routes.

---

## 1. Stale Documentation

Three documentation files have counts or classifications that no longer
match the code. These are not functional bugs but they make it harder to
reason about what is done.

| File | Location | What's stale |
|------|----------|--------------|
| `README.md` | line 9 | Already correct (`326 of 326 … Enterprise: 87/87`). Older snapshots referenced `29 of 87 (33%)`; verified current as of this change. |
| `AGENTS.md` / `CLAUDE.md` | line 7 | Already correct (`326 of 326 … Enterprise: 87/87`). Same older snapshot. |
| `crates/coder-server/PARITY_MATRIX.md` | entire file | Generated earlier than Waves 1–4. Several routes it lists as `stub-501` or `stub-partial` are now fully implemented (see §2 and §4 below). The document has been annotated with a pointer to this file and its Summary block corrected, but the per-route rows are still being regenerated out-of-band. |

Action for `PARITY_MATRIX.md`: the file now carries a "Partially stale" notice at
the top pointing at this document, and the Summary counts have been refreshed.
A full regeneration will happen with the next `coder-parity inventory` run.

---

## 2. `stub-501` Routes (5) — Classification Drift

`PARITY_MATRIX.md` still tags these five routes as `stub-501` ("handler
returns 501 or upgrades the WebSocket and immediately closes"). **All five
are now fully implemented** against the Go behavior. The documentation
drift is the gap; the code is not.

| Route | Method | Current behavior | Remaining semantic gap vs Go |
|-------|--------|------------------|------------------------------|
| `/debug/{user}/debug-link` | GET | `get_user_debug_link` — resolves the target user, checks it is an OIDC user, returns the user's claims (`crates/coder-server/src/handlers/auth.rs:903`). | None worth blocking on. |
| `/workspaceagents/{agent}/containers/watch` | GET | `get_workspace_agent_containers_watch` — subscribes to the agent-container pubsub channel, emits initial snapshot, streams updates (`crates/coder-server/src/handlers/agents.rs:548`). | Container list is always empty because `coder-connectivity` has no container introspection yet; devcontainer list is populated. |
| `/workspaceagents/{agent}/coordinate` | GET | `get_workspace_agent_coordinate` — RBAC-checks `Action::Ssh`, registers the peer in `TailnetCoordinator`, sends initial DERP map, multiplexes WebSocket ↔ coordinator channel (`crates/coder-server/src/handlers/agents.rs:693`). | Coordination frames use JSON, not the Go `tailnet.proto` DRPC wire format, so Go agents cannot speak this endpoint directly. |
| `/workspaceagents/{agent}/pty` | GET | `get_workspace_agent_pty` — verifies the agent is connected, sets up pubsub input/output channels, relays binary frames (`crates/coder-server/src/handlers/agents.rs:993`). | Reconnecting-PTY session resumption and signed-token validation are not wired; only in-flight relaying works. |
| `/workspaceagents/{agent}/watch-metadata-ws` | GET | `get_workspace_agent_watch_metadata_ws` — subscribes to metadata pubsub, emits snapshot, streams updates (`crates/coder-server/src/handlers/agents.rs:1221`). | None worth blocking on. |

**Work required:** update `PARITY_MATRIX.md` to reflect `complete` for these
routes; no handler code changes.

---

## 3. `stub-partial` Routes (2) — Actual Remaining Partials

PRs #199–#206 closed eight of the original ten `stub-partial` handlers
(scope enrichment, update-check probe, AWS/GCP instance identity,
workspace quota x2, workspace-sharing persistence, replicas listing).
Two genuinely-partial handlers remain; the rest of the old matrix's
`stub-partial` entries (`/buildinfo`, `/deployment/config`,
`/deployment/ssh`, GitHub/OIDC callbacks, agent external-auth) are not
actually hardcoded — they read from real config/store — and should move
to `complete` in the next regeneration.

| Route | Method | Current behavior | Real gap |
|-------|--------|------------------|----------|
| `/templateversions/{ver}/dynamic-parameters/evaluate` | POST | Returns the stored parameter definitions (`crates/coder-server/src/handlers/templates.rs:1568`). | Go runs the provisioner's `Plan` in preview mode with the user-supplied values to produce *evaluated* options. Rust returns the raw definitions because the provisioner runtime is not wired in. |
| `/workspaceagents/azure-instance-identity` | POST | Permissive path extracts `vmId` from the JWT payload without validating the signature (`crates/coder-server/src/instance_identity/mod.rs:109`). The `CryptoVerifier` path always returns `VerifyError::VerificationFailed` (`crates/coder-server/src/instance_identity/azure.rs:64`). | No PKCS7/Microsoft CA verification. Deployments running with `verify_instance_identity = true` cannot bootstrap Azure agents at all; deployments in permissive mode accept forged tokens. See Appendix A.1. |

---

## 4. Workspace Proxy Internal Stubs (6)

These live under `/api/v2/workspaceproxies/me/*` and are called by each
proxy replica. They are now real handlers (Wave 4B, PR #192) — the old
matrix still marks them `stub-partial`. The remaining gaps are narrower.

| Route | Method | Current behavior | Real gap |
|-------|--------|------------------|----------|
| `/workspaceproxies/me/register` | POST | Enterprise-gated, authenticates the proxy token, validates fields, updates proxy registration, upserts the replica row, returns siblings + DERP mesh key (`crates/coder-server/src/handlers/workspaceproxies.rs:490`). | DERP mesh key is derived from `state.app_signing_key` rather than `GetDERPMeshKey`; `derp_force_websockets` is hardcoded `false`. |
| `/workspaceproxies/me/deregister` | POST | Enterprise-gated, authenticates, deletes the replica row (`crates/coder-server/src/handlers/workspaceproxies.rs:622`). | Does not emit replica-change pubsub events, so sibling proxies don't learn about the deregistration in real time. |
| `/workspaceproxies/me/coordinate` | GET | Enterprise-gated, authenticates, upgrades to WebSocket, runs a ping/pong keepalive and logs inbound frames (`crates/coder-server/src/handlers/workspaceproxies.rs:667`). | No actual multi-agent tailnet coordination — the Go handler bridges the proxy into the coordinator so agent tunnels can egress via the proxy. This is the single largest proxy gap. |
| `/workspaceproxies/me/crypto-keys` | GET | Enterprise-gated, authenticates, validates the `feature` param against a whitelist, returns active keys, lazy-generates a 32-byte key if none exists (`crates/coder-server/src/handlers/workspaceproxies.rs:716`). | No rotation/expiry/retirement scheduler — keys are created once and never rotated. |
| `/workspaceproxies/me/issue-signed-app-token` | POST | Enterprise-gated, authenticates the proxy, re-authenticates the user via the embedded session token, signs an app token with `app_signing_key` (`crates/coder-server/src/handlers/workspaceproxies.rs:809`). | Does not run the full `WorkspaceAppsProvider` authorization chain (agent→app sharing level, template access, subdomain checks). |
| `/workspaceproxies/me/app-stats` | POST | Enterprise-gated, authenticates the proxy, inserts rows via `insert_workspace_app_stats` (`crates/coder-server/src/handlers/workspaceproxies.rs:898`). | None. |

---

## 5. OAuth2 Behavioral Gaps (2)

The OAuth2 provider flow is now largely implemented (PR #193), but two
items remain short of Go parity.

1. **Token revocation shape (`POST /oauth2/revoke`)** — the Rust handler in
   `crates/coder-server/src/handlers/oauth2.rs:1166` correctly follows RFC 7009
   (returning 200 even for unknown tokens) and tries both access- and
   refresh-token lookups, but it only deletes the token row. The Go
   implementation also cascades to dependent session tokens created from the
   OAuth2 token and emits an `audit_logs` entry per revocation.

2. **Dynamic Client Registration (RFC 7592) edge cases** — `post_oauth2_register`,
   `get_oauth2_client`, `put_oauth2_client`, `delete_oauth2_client` are complete,
   but the Rust code hashes the registration access token in best-effort fashion
   and returns 401 on lookup failure; the Go implementation has stronger
   replay protection (salted HMAC per client) and enforces the
   `software_id` + `software_version` redirect URI lock-step updates. Low
   priority unless external RFC 7592 clients are in scope.

---

## 6. Agent RPC Protocol Gap

This is the single largest subsystem gap in the backend. Go exposes the
agent↔coderd RPC surface as DRPC (Gogo-protobuf services) carried on a
yamux-framed WebSocket at `/api/v2/workspaceagents/me/rpc`. Rust upgrades
the same endpoint to a WebSocket and uses **JSON text messages** with a
custom message-type discriminator.

See `handle_agent_message` at `crates/coder-server/src/handlers/agents.rs:1901`
and `handle_agent_rpc_socket` at `crates/coder-server/src/handlers/agents.rs:1811`.

### Handled message types (5)

- `report_listening_ports` — updates `AgentProvider` port cache
- `report_stats` — inserts into `workspace_agent_stats`, replies with report interval
- `report_lifecycle` — persists lifecycle state + publishes pubsub event
- `update_metadata` — upserts metadata rows + publishes pubsub event
- `push_logs` — appends to `workspace_agent_logs` + publishes pubsub event

### Unhandled message types

At minimum the following DRPC RPCs on the Go `AgentAPI` are not handled
and fall through to the `_ => { debug!("unknown agent message type"); None }`
branch:

1. `get_manifest` / `GetManifest` — agent asks for its workspace manifest
   (env vars, scripts, apps, DERP map, tailnet keys). Without this a Go agent
   cannot configure itself on connect.
2. `get_announcement_banners` / `GetAnnouncementBanners` — agent polls for
   announcement banners to display in the terminal greeting.
3. `update_startup` / `UpdateStartup` — agent reports the result of its
   startup script, including expanded dirs and tailnet node preferences.
4. `batch_update_app_health` / `BatchUpdateAppHealth` — agent reports
   workspace-app health-check outcomes, which feed the workspace-status UI.

### Transport gap

Beyond the missing RPCs, the outer framing is different:

- No yamux multiplexing (Go uses one WS carrying many concurrent streams).
- No DRPC header/response envelope — replies are ad-hoc JSON objects
  (`{"type": "stats_response", "report_interval": 10}`).
- Outbound server→agent commands are plain text JSON on the same socket
  rather than DRPC replies keyed to a request stream ID.

A Go-compiled agent therefore cannot currently speak to a Rust coderd. This
is the blocker for running real provisioned workspaces end-to-end.

---

## 7. Provisioner / Template Gaps (2)

1. **Provisioner tag matching** — `app.rs:2068` uses
   `json_tags_contained_by(job.tags, input.provisioner_tags)` to pick
   eligible daemons, and all callers that create jobs currently pass
   `provisioner_tags: json!({})` (empty). The Go side uses a richer
   `ProvisionerTagSet` with wildcard (`*`) matching and scope tags
   (`scope: organization` vs `scope: user`). Until jobs are created with
   meaningful tags, non-default provisioners will not be reachable.
2. **Template dynamic-parameter evaluation** — see §3; the handler
   returns stored parameter definitions rather than driving the
   provisioner's Plan phase. This is adequate for templates that only use
   static parameters but wrong for any template that uses conditional
   `option` or `validation` blocks.

---

## 8. Workspace Quota Stub (1) — RESOLVED

Closed by **PR #202**. `compute_workspace_quota` now consults
`state.entitlements` for `FeatureName::TemplateRbac` and calls the
`get_quota_allowance_for_user` / `get_quota_consumed_for_user` queries
in `coder-db`. Both the deprecated `/workspace-quota/{user}` and the
organization-scoped
`/organizations/{organization}/members/{user}/workspace-quota` routes
report real allowance/consumed values.

Retained here for historical context so reviewers can trace the
resolution; no further work required.

---

## 9. Notification Dispatch Gaps (2)

The notification dispatch loop (`coder-notifications/src/lib.rs:119`)
correctly dequeues pending messages, dispatches per-method, and
increments the attempt counter / marks permanent failure. Two gaps
remain.

1. **SMTP email dispatch is a stub** —
   `coder-notifications/src/lib.rs:171` returns
   `NotificationDispatchError::Transport("SMTP email dispatch is not yet
   implemented".to_owned())` even when SMTP host is configured. Messages
   queued with `method=email` always retry and eventually land in
   `PermanentFailure`. Needs a `lettre`-backed sender.
2. **Webpush transport does not retry on transient failures** — the
   webhook path calls `http_client.post(...)` once, treats any non-2xx
   as a transport failure, and increments the attempt counter. Go
   distinguishes between retriable (5xx, timeouts) and terminal (4xx)
   failures and backs off exponentially; Rust uses a constant poll
   interval.

Inbox dispatch (`method=inbox`) and the webpush VAPID signing path are
complete.

---

## Priority Matrix

| # | Item | Category | Effort | Depends on |
|---|------|----------|-------:|------------|
| 1 | Port agent DRPC protocol (messages 1–4 + yamux + DRPC framing) | §6 | **L** (2–3 weeks) | Proto schema, yamux crate, tailnet DRPC types |
| 2 | Wire provisioner tag matching to job creation sites | §7.1 | **M** (3–5 days) | Go `ProvisionerTagSet` semantics |
| 3 | Dynamic-parameter evaluation via provisioner Plan | §3 / §7.2 | **M** (1 week) | Provisioner runtime in Rust |
| 4 | Azure instance-identity PKCS7/CA verification | §3 / Appendix A.1 | **M** (1 week) | Microsoft CA bundle, PKCS7 verifier |
| 5 | SMTP email dispatch (`lettre`) | §9.1 | **S** (1 day) | SMTP credentials in config (already present) |
| 6 | Workspace-proxy coordinate multi-agent tailnet bridge | §4 | **M** (1 week) | Agent DRPC (§6) first |
| 7 | Workspace-proxy signed app token: full `WorkspaceAppsProvider` chain | §4 | **M** (1 week) | Workspace-apps port from Go |
| 8 | Token-revocation cascade + audit entry | §5.1 | **XS** (hours) | Existing audit sink |
| 9 | Proxy crypto-keys rotation scheduler | §4 | **S** (1–2 days) | Background task runner |
| 10 | Regenerate `crates/coder-server/PARITY_MATRIX.md` | §1, §2, §4 | **XS** (hours) | `apps/coder-parity` |

*Earlier revisions of this matrix listed additional items that have
since landed and are therefore no longer tracked here:*
- *`/replicas`, workspace-sharing PATCH persistence, `/updatecheck` and
  `/auth/scopes` — closed by PRs #199, #200, #203, #205.*
- *Quota enforcement (§8) — closed by PR #202.*
- *AWS/GCP instance-identity cryptographic verification — closed by PR
  #206. Only Azure remains (item #4 above).*

Effort key: XS < 1 day · S = 1–3 days · M = ~1 week · L = ~3 weeks.

**Critical-path observation:** Item #1 (agent DRPC) is the blocker for any
scenario that involves running real workspaces against a Rust coderd. #7
piggybacks on it. Everything else is independent and can be tackled in
parallel.

---

## Consolidated Item Count

| Category | Items | Notes |
|---|---:|---|
| §1 Stale documentation | 3 files | README/AGENTS already correct; PARITY_MATRIX needs regen |
| §2 `stub-501` classification drift | 5 routes | All five handlers are actually complete |
| §3 Genuine `stub-partial` routes | 2 routes | Dynamic-parameters evaluation and Azure instance identity. The other 8 entries were closed by PRs #199–#206. |
| §4 Workspace-proxy internal stubs | 6 routes | Now implemented; remaining gaps are narrow (DERP mesh key, rotation, coordinate bridge) |
| §5 OAuth2 behavioral gaps | 2 items | Revocation cascade + RFC 7592 replay protection |
| §6 Agent RPC protocol | ≥4 message types + DRPC/yamux framing | Largest single gap |
| §7 Provisioner / template | 2 items | Tag matching, dynamic-parameter evaluation |
| ~~§8 Workspace quota~~ | — | **RESOLVED** (PR #202) |
| §9 Notification dispatch | 2 items | SMTP not implemented, webhook retry policy |
| **Total** | **~26 discrete work items** across ~16 routes and 3 subsystems |

Most items are small (XS/S). The long pole is the agent DRPC port (§6).

---

## Appendix A — Instance-Identity Follow-Ups (merged from main)

PR #206 landed the AWS + GCP verifiers and the Azure scaffolding. Two
narrow follow-ups remain; these supplement §3.2 above with the concrete
mechanics.

### A.1 Azure instance-identity: PKCS7 verification not yet implemented

**Location:** `crates/coder-server/src/instance_identity/azure.rs`

**Go reference:** `coder/coderd/azureidentity/azureidentity.go`

Standard Azure IMDS attested-data
(`http://169.254.169.254/metadata/attested/document?api-version=…`)
returns a **base64-encoded PKCS7/CMS envelope**, not a JWT. The Go
reference decodes the PKCS7, walks the cert chain to a bundled set of
Microsoft intermediate CAs, matches the signer cert's
`Subject.CommonName` against
`^(.*\.)?metadata\.(azure\.(com|us|cn)|microsoftazure\.de)$`, and reads
`vmId` from the inner JSON content.

The Rust port of the workspace-agent bootstrap endpoint currently
**short-circuits every Azure verification call to
`VerificationFailed`** with a `WARN`-level trace event. With
`verify_instance_identity = true`, the Azure endpoint will reject every
request. Failing closed is deliberate — silently accepting unverified
Azure tokens would be an identity-forgery vector.

**Consequences today:**

| Configuration | Azure bootstrap behaviour |
|---|---|
| `verify_instance_identity = false` (default) | Permissive verifier parses the JWT body and extracts `vmId`. Same behaviour as before this gap was introduced. |
| `verify_instance_identity = true` | Always returns 401. AWS + GCP verification still work against their real platform keys. |

**Work required to close the gap:**

1. Pick a Rust PKCS7/CMS crate (`cms` or `rasn-cms`) and add it to
   `[workspace.dependencies]`.
2. Port the Go PKCS7 parse + verify path, including signer-cert chain
   walk up to a bundled Microsoft intermediate CA set.
3. Apply the signer-cert `Subject.CommonName` pattern match (the
   `metadata.*` regex — which is *not* a JWT `iss` pattern; see the
   module docstring for why the earlier JWT-issuer approach was wrong).
4. Extract `vmId` from the inner JSON content and return it as the
   verified instance identifier.
5. Replace the short-circuit with a real call. The module already
   compiles a ready-to-use Entra ID issuer allow-list
   (`DEFAULT_ISSUER_REGEX`) for any future JWT-based managed-identity
   token exchange Coder chooses to layer on top of PKCS7.

**Why this is deferred:** the PKCS7 path requires (a) selecting and
adding a CMS crate, (b) bundling the Microsoft intermediate CA set
(Go ships them in `coder/coderd/azureidentity/azureidentity.go` as
embedded PEMs), and (c) writing a conservative cert-chain walker. This
is a real feature, not a one-line fix, and lands in a follow-up PR.

### A.2 AWS instance-identity: bundled certs do not cover every partition

**Location:** `crates/coder-server/src/instance_identity/aws.rs`

**Go reference:** `coder/coderd/awsidentity/awsidentity.go`

The `DEFAULT_CERTIFICATES` list ships 14 regional certs, matching the
Go reference (public commercial + 13 additional regions including
GovCloud and `cn-northwest-1`). The `Other` commercial-region cert is
refreshed ahead of the Go reference, which still carries the 2014 cert
that expired 2024-06-05.

**Still not covered by the bundled defaults:** any partitions Go has
not added yet (e.g. `us-iso*`, `us-isob*`, `eu-isoe*`, or
freshly-launched regions before we pick up new certs). Operators on
those partitions must inject additional PEM certs via either:

* `--aws-instance-identity-certs-dir <DIR>` on the CLI, or
* the `CODER_AWS_INSTANCE_IDENTITY_CERTS_DIR` environment variable, or
* programmatically through `AwsInstanceVerifier::with_certificates`.

The directory is scanned once at startup; `*.pem` / `*.crt` files are
appended to the bundled set, with successful loads logged at `INFO` and
parse/IO errors at `WARN`.

**Work required to close the gap (for a given partition):** add the
partition's current AWS-published RSA cert to `DEFAULT_CERTIFICATES`
with `notBefore` / `notAfter` annotated inline, following the Go
reference layout. This is a mechanical change per partition and is not
tracked here beyond the pointer to the AWS docs:
<https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/regions-certs.html>.
